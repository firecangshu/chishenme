# 验收用例（references/samples.md）

用于封装前的 dry-run 走查。逐条按 logic.md 推演，确认 触发档 / 动机型 / 硬过滤 / 模式 / 兜底 正确。

| # | 输入 | 期望行为 |
|---|---|---|
| 1 | 午休独自，预算20，不吃辣 | Top3 全不辣、price≤20、time 够午休 |
| 2 | "随便" | A 型秒给 1 默认+理由，不追问 |
| 3 | 海鲜过敏 | 含海鲜 avoid 项 100% 排除 |
| 4 | 夜宵 | 仅 scene 含夜宵项 |
| 5 | 连吃两天 | 不重复同一 item_id（去重生效） |
| 6 | 情侣约会 | D2：推环境好/可分享/偏精致项 |
| 7 | 商务宴请，领导不吃牛 | D3：升预算、招牌硬菜、提示确认主宾忌口、牛肉全排除 |
| 8 | 3人聚餐，一人海鲜过敏一人不吃辣 | D4：avoid 并集=[海鲜,辣]，两项全排除 |

### E 类（盲盒老虎机专项）

| # | 输入 | 期望行为 |
|---|---|---|
| 9 | 摇一个盲盒 | E 路径：先盲 SVG（候选数+三轮"?"）再开 SVG+文字揭晓；转轮符号==过滤后候选池 |
| 10 | 来个转盘 | 同 E（转盘作 spin 同义），三转轮符号 == 候选池，忌口项不出现 |
| 11 | 开一个随机的 | 同 E |
| 12 | （接9）再摇一个 | 再发盲→开，winner 排除上次已摇项；最多到第 3 次 |
| 13 | （接12）再摇×4 | 第 4 次锁定"这个吧，别纠结了"（兜底4） |
| 14 | （A 误判）"随便"→"不，摇一个" | A→E 改判，转盲盒（兜底1） |
| 15 | 海鲜过敏 + 摇一个 | pool 先剔除海鲜 avoid 项，三转轮只含安全项（硬过滤仍生效） |
| 16 | 微信小微「摇一个」(不渲染 SVG) | 降级为纯文本字符画转轮(slot-svg §7)，末行"🎉 开！→"给出文字中奖项，答案正确 |

## 走查记录模板

每条用例记录：触发档命中 → 动机判型 → 硬过滤结果 → 模式输出 → 兜底是否触发 → 期望 vs 实际。
